import { PriceItemPipe } from './price-item.pipe';

describe('PriceItemPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceItemPipe();
    expect(pipe).toBeTruthy();
  });
});
