import { ScoreUserPipe } from './score-user.pipe';

describe('ScoreUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreUserPipe();
    expect(pipe).toBeTruthy();
  });
});
