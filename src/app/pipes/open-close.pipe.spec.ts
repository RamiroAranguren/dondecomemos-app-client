import { OpenClosePipe } from './open-close.pipe';

describe('OpenClosePipe', () => {
  it('create an instance', () => {
    const pipe = new OpenClosePipe();
    expect(pipe).toBeTruthy();
  });
});
