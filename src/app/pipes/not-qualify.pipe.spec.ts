import { NotQualifyPipe } from './not-qualify.pipe';

describe('NotQualifyPipe', () => {
  it('create an instance', () => {
    const pipe = new NotQualifyPipe();
    expect(pipe).toBeTruthy();
  });
});
