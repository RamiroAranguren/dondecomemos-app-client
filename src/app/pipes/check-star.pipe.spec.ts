import { CheckStarPipe } from './check-star.pipe';

describe('CheckStarPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckStarPipe();
    expect(pipe).toBeTruthy();
  });
});
